### Initial setup

I ran schema.sql
But then ran into RSL issues when trying to login as a user in the app.

I ran into RSL issues

Finally I asked it to give me a updated schema.sql to start the project with...
Now is what you see in the repo.

### Supabase edge function

This allows us to create user accounts.
