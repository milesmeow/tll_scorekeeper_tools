### Initial setup

I ran schema.sql
But then ran into RSL issues when trying to login as a user in the app.

Then I was given:
fixing_rls_issues.sql to run.
Make sure you run (1) first.
Then run (2) with the super user UUID from the Supabase record
